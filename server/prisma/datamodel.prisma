type User {
    id: ID! @unique
    firstName: String
    lastName: String
    email: String! @unique
    password: String!
}

type Item {
    id: ID! @unique
    title: String!
    description: String!
    image: String!
    price: Int!
    user: User!
}
