import Head from 'next/head';

const Shop = () => (
    <div>
        <Head>
            <title>Hype-gear || Shop</title>
        </Head>
        <div>
            Show items here
        </div>
    </div>
);

export default Shop;
