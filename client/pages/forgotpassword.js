import Head from 'next/head';
import ForgotPassword from '../components/ForgotPassword';

const ForgotPasswordPage = props => (
	<div>
		<Head>
			<title>Hype-gear || Forgot password </title>
		</Head>
		<ForgotPassword />
	</div>
);

export default ForgotPasswordPage;
